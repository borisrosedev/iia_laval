// GIRARD ERWAN
// Licence 3

// RSX102 Technologies pour les applications en réseau

// TP : WebSocket

// Il y a de nombreuse dépendance à importer dans le code afin de faire un server WebSocket.

// Les dépendance nécéssaires :

// const express = require('express');
//     • Il permet de créer la base de l’API et le serveur Web
      
// const { createServer } = require('node:http');
//     • Il permet de créer un server HTTP
      
// const { Server } = require('socket.io');
//     • Il permet l’utilisation de WebSocket dans le serveur


// La gestion de connexion d’un WebSocket :

// io.on('connection', (socket) => {
//   console.log('a user connected');
//   socket.on('disconnect', () => {
//     console.log('user disconnected');
//   });
// });

//     • Cette partie permet de lire et donc savoir dès qu’il ya une connexion par Socket.io.
      
//     • On peux ensuite ajouter des bloc comme des console.log pour gérer les connexions, déconnexion, etc ...




// Le démarrage d’un serveur :

// server.listen(3000, () => {
//  console.log('server running at http://localhost:3000');
// });

//     • Cette partie nous permet de lancer le serveur et d’écouter sur un port donner ( ici le port 3000).
      
      

// Transmettre un action par Socket :

// socket.emit('chat message', input.value);

//     • Cette partie permet de communiquer entre les fichier HTML et les fichier JS .
// Celui si  envoie une donnée qui est relié a « chat message » et le back lit ce les message correspondant a ce paramètre.
//       Cette même méthode peux être utilisée pour faire le sens inverse qui du back vers le front.

// Récupérer les données coté client :

// socket.on('hello', (arg) => {
//   console.log(arg); // 'world'
// });

// Recuperer en temps réel les information à la reconnexion:

// const io = new Server(server, {
//   connectionStateRecovery: {}
// });